{% extends 'admin/base.html.twig' %}

{% block title %}ADMIN - Tag view{% endblock %}

{% block body %}
    <div class="row row-admin">
        {{ include('admin/alert.html.twig', {'type': 'success'}) }}
        {{ include('admin/breadcrumb.html.twig', { 'crumb': 'Tags', 'active': 'View' }) }}
        <div class="panel panel-default">
            <div class="panel-body">
                <div class="row">
                    <div class="col-md-offset-4 col-md-4 col-xs-offset-2 col-xs-8 text-center">
                        <div class="row text-left">
                            <p><b>Text:</b> {{ tag.text }}</p>
                        </div>
                    </div>
                </div>
                <div class="row row-actions">
                    <div class="col-md-4 col-xs-4 text-left">
                        <a href="{{ path('app_admin_tag_list') }}" class="btn btn-default"><span class="glyphicon glyphicon-arrow-left"></span>&nbsp;&nbsp;Back</a>
                    </div>
                    <div class="col-md-4 col-xs-4 text-center">
                        <a href="{{ path('app_admin_tag_delete', {'id': tag.id}) }}" class="btn btn-danger"><span class="glyphicon glyphicon-remove"></span>&nbsp;&nbsp;Delete</a>
                    </div>
                    <div class="col-md-4 col-xs-4 text-right">
                        <a href="{{ path('app_admin_tag_update', {'id': tag.id}) }}" class="btn btn-default"><span class="glyphicon glyphicon-pencil"></span>&nbsp;&nbsp;Update</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
